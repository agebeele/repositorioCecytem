<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo rojo -->
    <item android:id="@android:id/background">
        <shape>
            <solid android:color="@color/white" />  <!-- Cambia el color según tus preferencias -->
        </shape>
    </item>
    <!-- Progreso blanco -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <solid android:color="@color/verde_3" />  <!-- Cambia el color según tus preferencias -->
            </shape>
        </clip>
    </item>
</layer-list>